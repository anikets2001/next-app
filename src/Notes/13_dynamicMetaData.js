/*---

meta data and dynamic metadata is used to improve the seo of a web app,

static meta data is used global title and description of our whole app, 
if we want  a title and description separated for each page then we can generate dynamic metadata

step1: in a server component create and export a function generateMetadata()

export function generateMetadata(){
    return {
        title: 'Page title',
        description: 'Page description'
    }
}

---*/
